# Tour API
The API is built in NodeJS, Express and Sequelize. It is built on a MySQL database as a data source. In the Data folder you will find a mysql dump of the database that you can run in your local database.

I built it as a personal project.

MIT license!